kind: PersistentVolume
apiVersion: v1
metadata:
  name: flask-pv
  namespace: flask
  labels:
    app: flask
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-storage
  hostPath:
    path: /mnt/data
